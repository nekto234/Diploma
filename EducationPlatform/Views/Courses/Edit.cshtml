@model EducationPlatform.Models.ViewModels.Courses.CourseViewModel

@{
    ViewData["Title"] = "Редагування курсу";

    ViewData["Action"] = "Edit";
}

<div class="row">
    <div class="col-xl-6 col-lg-8 col-md-10 col-sm-12 col-xs-12 stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Редагувати курс</h4>
                @await Html.PartialAsync("_EditForm", Model, ViewData)
            </div>
        </div>
    </div>
</div>
